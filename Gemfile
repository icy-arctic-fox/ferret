source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

gem 'rugged', submodules: true
gem 'simplecov', require: false, group: :test

# Specify your gem's dependencies in ferret.gemspec
gemspec
